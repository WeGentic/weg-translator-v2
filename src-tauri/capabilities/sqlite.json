{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sqlite-db",
  "description": "SQLite access for translation persistence via tauri-plugin-sql.",
  "windows": ["main"],
  "permissions": [
    "sql:allow-load",
    "sql:allow-select",
    "sql:allow-execute",
    "sql:allow-close"
  ]
}
